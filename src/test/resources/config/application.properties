# Settings used only by tests.
collabflow.jwt.secret=test-only-secret-that-is-at-least-32-characters-long
collabflow.admin.email=admin@test.local
collabflow.admin.password=test-admin-password
# The background reminder job stays off; the tests run a batch themselves.
collabflow.reminders.enabled=false
